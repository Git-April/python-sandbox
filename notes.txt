Things that are new or easy to forget are noted in the files with #new
Here are some notes on those Things

Print can have an "end" as a final statement to override the default \n . This allows the next print statement to carry on in the same line instead of the next line.
Example:
print("Hello World!", end=" ")

Variables are case sensitive (I rarely need to think about this).
Example:
a = 4
A = "Sally"

Variables must start with a letter or an underscore, not a number (I rarely need to think about this).

Multiple variables can be assigned to multiple values at the same time.
Example:
x, y, z = "Orange", "Banana", "Cherry"

Multiple variables can be set to the same value at the same time.
Example:
x = y = z = "Orange"

Unpacking: Values in a list or tuple can be extracted into variables all in one line.
Number of values being unpacked into variables much match the number of values in the list or tuple, otherwise it'll throw an error.
Example:
fruits = ["apple", "banana", "cherry"]
x, y, z = fruits

Variable of the same name inside of a function overrides global.
If it exists in the function, it must be defined before it is used - the global one will not be accessed and instead throw an error because it has not been defined yet, knowing that there exists a local variable.
Example:
x = "awesome"
def myfunc2():
    x = "fantastic"
    print("Python is " + x)
myfunc2()

Declaring a variable as global inside of a function allows it to be accessed outside of the function after the function is called.
This can also be used to change an already-defined global variable initialized outside of the function.
Example:
def myfunc3():
    global myfunc3variable
    myfunc3variable = "fantastic"
myfunc3()
print("Python is " + myfunc3variable)

j is used as the letter for representing a complex number (not i like in math contexts)
Example
x = 1j

On lists [], sets {}, and tuples ():
Mutable: Elements can be modified in lists and sets, but not in tuples
Ordered: The order of elements is maintained in lists and tuples, but not in sets.
Duplicates: Lists and tuples can have duplicates, sets cannot.
Different data types: Allowed in lists and tuples, not in sets.
Lists and tuples are the same except that lists can have elements modified, tuples cannot.
Sets are different from lists in that the order of elements is not maintained, duplicates are not allowed, and every element has to have the same data type.
A frozenset then is an immutable set.

When explicitly declaring the list type (and similar for tuples and sets), list() takes an iterable parameter (a tuple is an example of an iterable) and converting it to a list

Bytes and bytearray datatypes:
Bytes cannot be changed (immutable), bytearray can.
Both deal with encoding. More advanced topics to dive into as needed.
Memoryview seems related and can help to improve runtime.

NoneType and None are useful in dealing with functions with no return value.

Walrus operator := means to set the variable to the value as part of a larger operation, such as within a print statement or an if statement
Examples:
print(x:=3)
numbers = [1, 2, 3, 4, 5]
if(count := len(numbers)) > 3:
    print(f"List has {count} elements")

Ternary operator allows inline if/else statements. Something about this reminds me of SQL case statements.
Example:
num = 6
x = "Fri" if num == 5 else "Sat" if num == 6 else "Sun" if num == 7 else "weekday"

Is operator check if both values point to the same object in memory (compare to ==, which checks if the variables ar equal)
Example:
x = ["apple", "banana"]
y = ["apple", "banana"]
z = x
print(x is z)
print(x is y)

List Comprehension: a simpler way to iterate through a list
Examples:
thislist = ["apple", "banana", "cherry"]
[print(x) for x in thislist]
fruits = ["apple", "banana", "cherry", "kiwi", "mango"]
newlist = [x for x in fruits if "a" in x]
print(newlist)

Remember: when specifying a range like [2:5], the end position is not included in the result

To make a one-item tuple, it has to be followed by a comma, otherwise it wont be recognized as a tuple
Example:
thistuple = ("apple",)
print(type(thistuple))

In unpacking tuples, the * means to assign the remainder of the variables to that variable as a list, based on positioning in the unpack
Examples:
fruits = ("apple", "banana", "cherry", "strawberry", "raspberry")
(green, yellow, *red) = fruits
print(green)
print(yellow)
print(red)
fruits = ("apple", "mango", "papaya", "pineapple", "cherry")
(green, *tropic, red) = fruits
print(green)
print(tropic)
print(red)

Tuples can be multiplied by integers to double it
Example
fruits = ("apple", "banana", "cherry")
mytuple = fruits * 2
print(mytuple)

1 and True are treated as the same value in sets. 0 and False are treated as the same value in sets.
Examples:
thisset = {"apple", "banana", "cherry", True, 1, 2}
print(thisset)
thisset = {"apple", "banana", "cherry", False, True, 0}
print(thisset)

Remove on a set will error if the item does not exist. Discard will not error.
Example:
thisset = {"apple", "banana", "cherry"}
thisset.discard("banana")
print(thisset)
thisset.discard("banana") #new
print(thisset)

Pipe operator in sets functions as a union
Example:
set1 = {"a", "b", "c"}
set2 = {1, 2, 3}
set3 = set1 | set2
print(set3)

Note: union creates a copy of a set, update changes the set in place

Carrot operator opperates as symmetric_difference in a set
Example:
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}
set3 = set1 ^ set2
print(set3)